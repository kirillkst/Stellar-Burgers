import PropTypes from 'prop-types';
import styles from './app.module.css'; 


App.propTypes = {
  
};

function App(props) {
  return (
    <div className={styles.wrapper}>
      
    </div>
  );
}

export default App;
